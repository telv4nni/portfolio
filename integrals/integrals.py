import csv
import sys
import random
import math

# Create Integrals Formulas for calculation

# Count integral x^n dx
def ixndx(x, power):
    x = x
    n = power
    integral = None

    integral = string(x^(n+1))/n+1 + C

    return integral

