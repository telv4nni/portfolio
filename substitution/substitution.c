#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int main(int argc, string argv[])
{
        if (argv[2] =/= NULL)
        {
            printf("Usage: ./substitution key")
        }
    string plaintext = get_string("Plaintext: ");
    string ciphertext;
}